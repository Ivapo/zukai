# Review record — `zk-012` link-length

**Append-only.** An earlier round is never edited, even where a later one proved
it wrong — a round that was wrong is part of the record, and rewriting it
destroys the property that makes this a record rather than a summary.

This tree is excluded from the spec checks and from the citation ratchet
(`.spec-lint.yaml`'s `exclude_trees`).

## Rounds

### Round 2 — Phase 1 only — 2026-08-09 — same reviewer, resumed — **`VERDICT: READY`**

Zero blocking findings; converged. The reviewer verified every adjudication
against the file rather than the changelog, and re-checked the rewritten gate 3
against the code: `moveNode` writes only `doc.layout.nodes` and a
`setLinkLength` on `setMarkingLane`'s shape writes only `doc.links`, so both
reference assertions hold for a correct implementation and fail for the
regression §2.2 fears. One observation recorded below the bar, no change
required: §2.2's "an untouched layout *is* an untouched drawing" is mildly
overbroad (the drawing also derives from `link.lanes` and `median_gap`), but as
the regression test for *this* invariant the layout-identity assertion is the
right and sufficient form, and gate 5's dev pass covers the picture end to end.
It also noted the §2.6 "finite positive" bound coherently excludes typing `0`,
matching §2.1's "absent … is not zero" and Phase 2's None-rather-than-0 posture.

**Phase 1 `reviewed: 2026-08-09`; `status: accepted`.** Phase 2 has not had a
round and stays blocked.

### Round 1 — Phase 1 only — 2026-08-09 — clean-room reviewer with repo access — **`VERDICT: NOT READY`** (1 blocking)

**Round 0** (author, recorded with this entry): Phase 1 produces the observable
directly — the label `1800m` drawn on the canvas and in the exported figure, and
the figure is what this project exists to produce. It is the right first phase:
`CLAUDE.md` names this exact example as the founding idea, and the phase builds
the smallest thing that makes it true. Proceed.

Grounding came back fully clean — every `file:symbol` the phase cites was opened
and matched the code (`graph.rs:Link`, `mod.rs:SCHEMA_VERSION`, `types.ts:Link`,
the six `geometry.ts` symbols, `Diagram.tsx:needsText` with exactly two arms,
`measureDiagram`'s font await gated on the same predicate, the Inspector's
`if`-chain tail, `setMarkingLane`'s key-dropping shape, `coalesceKeyFor`'s
empty-string carve-out, and all three rules citations). The reviewer re-derived
the Goal's numbers (`9/3.5 = 2.571`; `500 × 2.571 / 9 ≈ 143`) and confirmed
"SCHEMA_VERSION stays 2" is the model's own documented rule for an elided
optional field.

**Blocking, fixed:**

1. **OQ-1 left Phase 1's central persisted type formally unsettled** — the
   design committed to `Option<f64>` while OQ-1 stood `needs-input`, so an
   implementer building from the spec alone could not know whether the `f64`
   decision stands or the "text" framing wins, in a versioned save format.
   Escalated to the human as the question demanded; **the user chose the
   number** (f64 metres, formatted at draw). OQ-1 is marked RESOLVED with the
   reasoning, and §2.4's dangling reference to it now points at the resolution.

**Non-blocking, adjudicated:**

2. **Accepted** — gate 3's "asserted by reference" could not literally hold on
   `drawnPolyline`'s return value (`linkPolyline` mints a fresh array per call).
   §2.2 and gate 3 now name the assertable form: `doc.layout` identical by
   reference after `setLinkLength`.
3. **Accepted** — §1 mis-attributed the scale disproof to zk-009's Phase 3; it
   was recorded at that spec's export cut (its §0). Attribution fixed, numbers
   unchanged.
4. **Accepted** — the formatter's rounding was unspecified; §2.4 now says
   "rounded to the nearest metre".
5. **Rejected** — "the label's side of the road is never chosen". OQ-2 already
   records this as decide-in-the-app discretion, the posture every marking
   constant took, and the dev-pass gate pins no side. Naming a side in the spec
   now would be a guess the app exists to answer.
6. **Accepted** — unparseable Inspector input was unspecified; §2.6 now states
   that text parsing to no finite positive number dispatches nothing, the rest
   being implementer discretion under the UI-is-provisional posture.
