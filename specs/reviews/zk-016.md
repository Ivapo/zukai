# Review record — `zk-016` bus-stops

**Append-only.** An earlier round is never edited, even where a later one proved
it wrong — a round that was wrong is part of the record, and rewriting it
destroys the property that makes this a record rather than a summary.

Rounds are newest-first.

## Rounds

### Round 3 — 2026-09-11 — scope and gates resumed — **READY** (converged)

**Zero blocking from both.** The spec is `accepted` and both phases carry
`reviewed = 2026-09-11`: a document-wide round converging clears the document. The
loop resumes whichever reviewer raised a finding — scope, whose round-2 blocker this
was, and gates, whose literals the fix changed. Grounding had returned READY in round
2 and nothing in its lens moved, so it was not resumed.

- **The round-2 blocker is resolved.** Scope confirmed §2.7.1 against the file, then
  tested its one load-bearing claim — "a line to borrow always exists" — against the
  code: `RoadShape` draws both edge lines unconditionally; a divider is dropped only
  where `laneLineOffsets` claims a boundary on exactly the terms `laneLine` draws one,
  and `laneLine`'s one extra skip skips the road too; band 0's inner edge is the
  offside edge line on a one-lane road and the solid shoulder line beside a shoulder;
  and a bay's outer edge and mouth lines both span the box's whole stretch. Two edges
  noted, each still with a line: a `double` lane line straddles its boundary by two
  units, and an in-lane stop overlapping another stop's bay borrows that bay's mouth
  line for its cut kerb edge.
- **Gates replayed the geometry** — `pointAlongPolyline`, `markingBar`, the slide — on
  every changed fixture and re-derived each literal exactly; confirmed `150` and
  `200 / UNITS_PER_METRE` round-trip exactly, so the overlap clause's endpoints compare
  exactly; showed the perpendicular-end-bar clause fails a build that orients both bars
  off the centre's direction (−9 on the far bar of a bent fixture); and showed every
  non-merging cut it could construct fails "exactly two pieces" or their endpoints. All
  eight Phase 1 and four Phase 2 mutations fail a named clause.

**Non-blocking, raised by both, folded after convergence:** the §1 sketch still drew
closed boxes whose sides ran beside the road's lines — the layout §2.7.1 rejects — and
§2.6's opening still justified `polylineStretch` by the box's long sides. The sketch
now shows two end bars between the divider or the mouth line and the kerb or outer
edge line, and §2.6 says what the stretch places: the ends and the hit spine, then the
bay's asphalt, lines and cut.

**No rejections across the episode.** Three rounds and ten blockers: nine in round 1
(twelve raised, deduplicated), one in round 2 introduced by a round-1 fix, none in
round 3.

### Round 2 — 2026-09-11 — same panel, all three resumed — **NOT READY**

**Grounding and gates returned READY; scope returned NOT READY on one blocker, and
it was inside the author's own round-1 fix** — the pattern the loop names, a fix
introducing a blocker. All three verified from the file rather than the changelog:
every round-1 blocker is resolved as written, every literal the fixes keyed to
re-derived exactly, and the gates lens confirmed each of the eleven listed mutations
fails a named clause.

- **[BLOCKING] The bay box's traffic side was a solid stroke along the mouth.**
  Round 1's fix #7 moved the box's long sides onto the bay's lines so they stopped
  drawing 1.5 outside them — which put the traffic side exactly on the dashed mouth
  line, so a correct build painted 45 units of solid "do not cross" along the opening
  §2.9 exists to open, and failed its own dev pass. The author found the same flaw one
  phase earlier, unraised: the in-lane box's traffic side sits on the dashed lane
  divider. Resolved by neither fix the reviewer offered — declaring the solid side
  intended contradicts §2.9, and moving the sides off the lines brings back round 1's
  doubled strokes — but by §2.7.1 (decision, recorded): the box draws its two ends and
  the word, and the road's own lines are its long sides. Phase 1's gate asserts the
  stop paints no long side, with that mutation.

**Non-blocking, all accepted:** the dev-pass drag check (all three lenses — a hand
drag moves `position`, and the first drag of a repainted stop clears its `lane`, so
an undo step appears either way) replaced by the saved stop carrying no `lane` key;
which reach a Phase-1 `form: bay` takes (grounding, gates) — the in-lane one;
§2.3.1's wording on the goldens and on which documents start at the constant
(grounding); the overlap clause pinned to exact endpoints (gates), since an unmerged
cut can still leave "one gap"; `rules/canvas-interaction.md`, at its cap, added to
Phase 1's close-out (scope); `network-yaml.md`'s version table row and the one inline
anchor literal (gates); and §2.4's rejected alternative, which read against the chosen
design until the cascade's and the drag's clearing were named housekeeping (scope).

**One disagreement adjudicated.** Grounding said the close-out's "stated criterion
for moving a cap" is written nowhere; scope said it is. Both were half right —
`spec-authoring.md` and the sync-rules loop state none, while `link_bends_spec.md`
records "the cap moves only if the rule genuinely gains a mechanism". The close-out
now cites that line as the project's precedent and leaves the move to the human.

**No rejections.**

**Re-measured by the author for this round's fix:** in-lane end bars at `x` 37.5 and
82.5 spanning `y` 4.5–13.5, and at 75 and 120 when slid to the road's end; bay end
bars at `x` 97.5 and 142.5 spanning `y` 13.5–22.5; bays centred at 150 and 200 on a
400-unit road cut the merged `[103.5, 246.5]`, leaving pieces ending at 103.5 and
starting at 246.5; a bay at `position = 0` on the 240-unit road slides to 46.5 and
cuts `[0, 93]`, leaving one piece from 93.

### Round 1 — 2026-09-11 — panel of three, fresh, repo access — **NOT READY**

**Document-wide.** No phase has shipped, so `spec-authoring.md` §7 takes one round
over the whole document; the loop's `--phase` form would have scoped it to Phase 1,
but Phase 2 builds on Phase 1's model, stretch and anchor. Lenses: correctness and
grounding; exit-gate testability and numbers; scope and cross-file consistency.

**Round 0**, answered by the author: yes. Both phases draw on the road itself, in
the app and in an exported figure, so a reader of a figure sees where buses stop and
whether they pull out of the running lane — and it is the right picture, a lettered
box or a lay-by being how road drawings show a stop: paint and asphalt, not a
transit overlay.

**NOT READY from all three** (grounding 4 blocking, gates 5, scope 3). Deduplicated
to nine blockers, all accepted:

1. **§2.3's bump list missed two tests pinning `SCHEMA_VERSION == 2`** —
   `mod.rs:a_zkai_saved_with_movements_still_loads_and_writes_none` and
   `mod.rs:an_unstated_length_writes_no_key`. All three lenses. The author's own
   earlier grep had been cut off by `head -15` at exactly the line before them.
2. **A save never declared the current version** (grounding). `encode` wrote the
   document's own `schema_version` and both load paths keep the file's, so an
   example given a stop would save declaring 2 — the failure the bump exists to
   prevent, and already latent for version 2. Fixed by stamping in `encode` (§2.3.1),
   chosen over `decode`/`normalizeDocument` (breaks two pins) and a serde attribute
   (the struct crosses IPC and wasm as JSON on load). Verified: `save_document` and
   the wasm shell are `encode`'s only callers.
3. **`MarkingAnchor.along` was the unclamped distance** (grounding, scope; gates
   non-blocking). A stop past a shortened road's end drew a box of no length beside
   a word at the clamped anchor. Fixed as a clamped, required `distance` — renamed off
   `MarkingForm`'s `along` arm — and **OQ-4 resolved to the slide**, since the clamp
   also drew half a box with its word at the edge.
4. **A stale `lane` could make a marking invisible** (grounding). Stop in lane 2,
   road narrowed to two, repainted as a stop line: `markingAnchor` skipped it,
   unclickable, with the Span control that could fix it withheld while it was a stop.
   Fixed: the cascade keeps a stop and clears an outgrown `lane`, and a drag writes
   none — which also answers the scope lens's no-op undo step.
5. **Phase 1 hung on OQ-1 and OQ-6** (scope), both still open while marked as
   blocking it. Both resolved: OQ-1 on its proposal; OQ-6 rewritten, since four pins
   and no stamping made the proposed rewrite impossible.
6. **No clause pinned the stop's exemption from the out-of-range-lane skip** (gates;
   scope non-blocking). An implementation placing the stop term after the skip passed
   every clause while dropping a narrowed road's stop from the drawing. Clause added
   on a two-lane road, with the mutation.
7. **The bay's box sat 1.5 outside its own lines** (gates; scope non-blocking) — sides
   at `e` and `e + b` against the mouth line at `e − 1.5` and the outer edge line at
   `e + b − 1.5`, drawing doubled strokes. Moved onto the lines, matching the in-lane
   box, whose kerb side lies on the road's edge line.
8. **The offset-then-cut mutation failed no clause** (gates; grounding and scope
   non-blocking): every Phase 2 fixture was straight, and "coincides with the uncut
   line" holds under the mutation. Clause added on §2.6's own bent road.
9. **`BayShape` moved into the marking layer failed no clause** (gates; grounding
   and scope non-blocking): with one road and one stop the source order still held.
   The fixture now lists another road's marking first.

**Non-blocking, all accepted:** the close-out's missing rules — `junctions.md` and
`network-yaml.md` ("still **2**"), `diagram-export.md` (`needsText`'s terms, what is
fill), `road-markings.md`'s opening claim, `persistence.md` — with a line budget per
rule; comments naming version 2; `StopForm`'s derives; a defined `BusStopShape`, and
`MarkingShape`'s and `haloWidth`'s two-way `form.along` tests made three-way; §2.1's
reason for a new spec, which contradicted §2.2 and now rests on the work spanning
three subsystems; §2.2's reading of the `Selection` rules, which was backwards;
OQ-7 reachable in Phase 1 and resolved for both; distinguishing `road-bay` tokens so
a gate can select the bay's elements; the mouth line painted as a divider (OQ-8);
the hit target and halo following the box into the bay; merged cuts and no empty
edge path (OQ-5); a sub-`SAME_EDGE` fixture that the stated mutation actually fails;
the "zoomed out as far as it goes" dev check, replaced by 50%; the `strokeAllowance`
clauses dropped, since it reads only `doc.links` and cannot fail; and §2.6's
link-bends quote, which used "drawn" in the opposite sense from its sentence.

**No rejections.**

**Re-measured by the author in this pass**, for the clauses the fixes keyed to
numbers:

- Bent road `A(0,0)→B(50,0)→C(50,150)`, three lanes (`w` 30, `edgeInset` 13.5), bay
  at `s = 125`: cut stretch `[78.5, 171.5]`, all on the second leg. The first kept
  piece ends at `(36.5, 28.5)`, the second starts at `(36.5, 121.5)`; cutting the
  offset polyline instead puts the first end at `(36.5, 55.5)` (36.5 along its first
  leg, then 42).
- Straight `N1(0,0)→N2(240,0)`, three lanes, `s = 120`: casing edge `x` 73.5–166.5 at
  `y` 15, outer edge `x` 97.5–142.5 at `y` 24, bay box sides at 13.5 and 22.5.
- The slide on the 120-unit road: in-lane `h` 22.5 puts a stop at the end at centre
  97.5, box 75–120; a bay's `h` is 46.5.
- `polylineStretch` fixture `[(0,0), (50,0), (50,5e-7), (50,100)]`: filtered, the
  point at 100 is `(50, 50.0000005)`; a walk counting the short segment lands 5e-7
  short.
- Confirmed from the gates lens, not re-derived: the divided two-lane pair's bay at
  `y` 24–33 against −6 to 3 when negated; band 0 of a three-lane arterial at offset 9,
  width 9, edges 4.5 and 13.5; `60 / UNITS_PER_METRE` converting back to 60.
