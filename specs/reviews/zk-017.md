# Review record — `zk-017` road-declutter

**Append-only.** An earlier round is never edited, even where a later one proved
it wrong — a round that was wrong is part of the record, and rewriting it
destroys the property that makes this a record rather than a summary.

Rounds are newest-first.

## Rounds

### Round 2 — Phase 2 only — 2026-09-14 — same clean-room agent, resumed — **READY** (converged)

**Zero blocking.** Phase 2 now has `reviewed = 2026-09-14`, and the spec stays
`accepted`.

**Both round-1 blockers are resolved, as confirmed against the file.**
- **Citations.** No spec citation names a symbol Phase 2 deletes. With all eight
  deleted symbols renamed on a copy, `spec-lint` reports 0 errors; round 1 got 7.
- **The three-outcome test rule, simulated end to end on a copy.** The copy carried
  the behaviour change (factors at 1, no class token, no minted view), with the
  delete set skipped and every rule-3 rewrite applied as worded. The result was 565
  passed, 15 skipped and 0 failed, and the skips are exactly the delete set
  (4 + 6 + 2 + 2 + 1). No literal moves under rule 2.

**Numbers re-derived:**
- the describe counts, 4 and 6;
- 3 "…at every lane count and class" titles;
- 5 × 2.8 m and 4 × 3.5 m both exactly 39, since a 2.8 m lane is 7.2 units, the old
  ramp lane;
- `TEXT_SIZE * CAP_HEIGHT` = 4.2 < `LANE_PX` = 9;
- 19 class-only example views (3 + 8 + 8);
- `diagram-export.md` at 304/305.

**Four non-blocking findings, all accepted and folded in before the date was set:**
- `export.tsx:strokeAllowance`'s doc comment joins the "Comments naming a class"
  bullet.
- The bus-bay case that "carries the road's class on its group" loses the class from
  its title and comment. The grep gate cannot see a title.
- The `import.rs` rewrite also removes the `links.len() == 3` assertion outside the
  loop.
- Edits forced only by a removed parameter are declared unlisted, because `tsc` names
  each one and each has a single fix.

### Round 1 — Phase 2 only — 2026-09-14 — fresh clean-room agent — **NOT READY**

**Round 0 (asked by the author, once for this episode): yes.** Phase 2 produces the
observable: a `ramp` or `local` road draws at full lane width on the one asphalt, in
the figure and on the canvas. It is the right change, because four class names drew
two looks. The narrower look also conflicts with how a reader counts lanes, and a
ramp's identity is already drawn by its gore, its chevrons and its lane count.

**Grounding verified clean:**
- every model, derivation, render and panel site §2.2 names, and every
  `linkStyle(doc, id)` call site (plus the Inspector's; see below);
- no `deny_unknown_fields`;
- exactly 8 class-only views in each import golden, whose removal is a pure deletion;
- the seven tests the gate named;
- Phase 1 moved nothing Phase 2 cites;
- the `git grep` command reproduces;
- zk-004's phase name matches exactly.

Baseline: 580 vitest green and `spec-lint` 0 errors.

- **[BLOCKING] The gate's "`spec-lint` 0 errors" could not pass.** §2.2 and Phase 2
  cited `layout.rs:LinkStyle`, `types.ts:LinkStyle`, `geometry.ts:classWidthFactor`
  and `document.ts:linkStyle`, all of which the phase deletes, and
  `CIT_SYMBOL_ABSENT` is an error. Renaming them on a copy gave 7 errors.
  **Resolved:** a deleted symbol is named in words (`LinkStyle` in `layout.rs`), and
  §2.2 states the rule.
- **[BLOCKING] The "deleted, not rewritten to pass" list was incomplete.** It named 7
  tests. It missed four pure-class cases, and the Rust
  `layout_is_seeded_with_defaults_rather_than_derived` asserted three `Arterial`
  views against the new "import writes no view" clause. It also missed the fixtures
  where the class carried a non-class claim: a taper case built on a 5-lane ramp,
  and a cap-height bound built on `classWidthFactor("ramp")`. It gave no rule for
  six view-shape assertions in `state.test.ts`, one of which would crash on
  `view(twice).align`. **Resolved:** the gate now sorts every test into one of three
  outcomes by what it asserts:
  - deleted whole: an exact set;
  - class leaves the fixture, assertions stand: a named set, where a moving literal
    is a finding;
  - rewritten as stated: each replacement given.

  The author re-measured the taper replacement (5 × 2.8 m = 39) and found a fourth
  class-loop case the round had missed, "…at every size", whose `ramp` was its
  narrow size. That case moves to rule 3 with 2.8 m lanes.
- **Seven non-blocking, all accepted:**
  - the `roadGroup` regex is named;
  - the class-naming comments the grep gate would force are named;
  - `Inspector.tsx:Inspector` joins the call-site list;
  - the supersession reason is corrected: only the `supersedes` half is rejected by
    `spec-lint`, and the cut half alone would leave a dangling `by`;
  - the close-out gains line budgets and `README.md`'s "road class";
  - the examples' `links:` key goes whole;
  - the dev pass opens the pre-change `motorway-ramp.zkai` rather than a nonexistent
    `style: local` file, and §1 stops crediting a taper `motorway-ramp` does not have.

### Round 2 — Phase 1 only — 2026-09-14 — same clean-room agent, resumed — **READY** (converged)

**Zero blocking.** Phase 1 carries `reviewed = 2026-09-14` and the spec is
`accepted`. Phase 2 has had no round and stays gated: this episode was scoped to
Phase 1 at the user's request, although a spec with no shipped phase would by
default take one document-wide round (§7).

- **The round-1 blocker is resolved.** The reviewer confirmed against the file that
  OQ-2 is RESOLVED, with its conditional sentence struck through and its reasoning
  kept, and that Phase 1's scope states it edits no `examples/*.zkai`.
- **Re-derived the new literals.** The apex-x clause: a one-lane road is `9 + 3 = 12`
  wide, so the arrow size is `max(6, 12 × 0.45) = 6`. `L1` eastbound from midpoint
  x 60 puts the apex at 63 and both base corners at 57, while `L2` westbound mirrors
  it, so the clause tells the two arrows apart. The bend-selection case matches
  `state.ts`'s `Selection` arm, and `isSelected`'s `"id" in sel` narrowing makes it
  false. The `interaction`-gate mutation therefore fails it, as claimed. The
  `interaction()` helper is top-level, unshadowed, and selects `L1`.
- **One non-blocking finding, accepted.** `diagram.css`'s header must name the arrow
  in words and never as `road-arrow`. `CHROME` matches the embedded stylesheet,
  comments included, so the token would fail all thirteen assertions. Folded into
  the scope's comments sub-bullet.

### Round 1 — Phase 1 only — 2026-09-14 — fresh clean-room agent — **NOT READY**

**Round 0 (asked by the author, once for this episode): yes.** Phase 1 produces the
observable, since every exported figure and the three landing figures lose their
arrowheads. It is the right one, because an arrowhead down the middle of a road is
not something a figure's reader asked for. The one consumer that needs the arrow,
the Inspector's travel-frame readout, exists only for a selected link, which is
where the arrow stays.

**Grounding verified clean.** Every checked citation matched the code:
- `RoadShape`'s ungated `arrow` and its `mid`, used by nothing else;
- `selected` false without `interaction` and for a bend selection;
- `.road-arrow` present only in `diagram.css`;
- `diagramInner` rendering without `interaction`;
- `CHROME` used by **13** assertions, all embedding `diagram.css`;
- `lengthLabel` measuring its own midpoint;
- `render-examples` rebuilding `dist/` before it checks;
- the committed figures carrying 3, 8 and 8 arrows (motorway-ramp, roundabout,
  signalized-cross);
- `roundabout.zkai` with no turn arrow and `signalized-cross.zkai` with 8.

Baseline 576 vitest tests green. The mutations were checked by reading only, since
the reviewer's scratch copy could not be edited.

- **[BLOCKING] OQ-2 "blocks Phase 1" and was unresolved.** Its fallback, adding turn
  arrows if the roundabout "reads ambiguous", was a taste gate with an unscoped edit
  to a landing-figure document behind it. **Resolved:** OQ-2 is RESOLVED as no, and
  Phase 1 edits no example. The roundabout's loss of direction is named and accepted,
  and painting turn arrows onto it later is a document edit that needs no spec.
- **Seven non-blocking, adjudicated:**
  - **Accepted:** a named two-way fixture, with an apex clause proving which link's
    arrow draws.
  - **Accepted:** a bend-selection unit case, with the dev pass adding a bend first.
  - **Accepted as a named gap:** a rule deleted rather than moved. The dev pass
    requires a white arrowhead, and the phase starts no test reading `styles.css`.
  - **Accepted:** the close-out names both chrome inventories and their line budgets.
  - **Accepted:** the stale comments go into scope (`alignmentReading`, the
    `diagram.css` header, `CHROME`'s "ten").
  - **Accepted:** §2.11 corrected to §2.11.3.
  - **Accepted:** the §4 preamble's "disjoint" claim is corrected to name the shared
    `w * 0.45` expression.
- **Rejected:** deleting `geometry.ts:endDirection`, dead since `67f9018`. It is a
  code-only cleanup independent of this phase (§6.1 step 0), so it can be its own
  commit rather than a clause of this gate.
